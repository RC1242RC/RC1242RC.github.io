name: Test

on:
  workflow_dispatch:
    inputs:
      test:
        description: 'test'
        required: true
        default: 'test'

jobs:
        
  document_repos:
    runs-on: ubuntu-latest
      
    steps:
      - name: Set the value
        run: |
          echo "action_state=${{ inputs.test }}" >> $GITHUB_ENV
      - name: Use the value
        run: |
          echo "${{ env.action_state }}"
        
        
